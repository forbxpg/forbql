# forbql
Secure Text-to-SQL agent. Ask your PostgreSQL in plain language - the agent can't break it: AST validation, EXPLAIN cost limits, read-only execution, PII masking, audit log and self-correcting SQL. CLI, FastAPI (SSE) and MCP server.
